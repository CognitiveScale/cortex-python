builder:
  image: python:3.10-bullseye
  command: bash -cx 'apt-get update
    && apt-get install -y jq
    && make dev.install
    && make test
    && make build
    && make docs.dev
    && make docs.package'
  environment:
    - ALPHA_BUILD="true"
